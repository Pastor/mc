package mc.minicraft.component.sound;

import mc.api.Sound;

public final class SoundManager implements Sound {
    @Override
    public void play(Type type) {
        switch (type) {
            case BOSS_DEATH:

        }
    }
}
